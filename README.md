# claude-router

Single-endpoint router for Claude Code: exposes several Anthropic-API-compatible backends as one gateway and lets you switch models mid-session.

## Build

```sh
cargo build
cargo test
```
